name: Create and Publish File

on:
  push:
    branches:
      - main

jobs:
  build_and_publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Generate files
        run: |
          touch dynamic-file.txt

      - name: Commit and push changes
        run: |
          git config user.name 'GitHub Actions'
          git config user.email 'actions@github.com'
          git checkout gh-pages # Create a new branch for the changes
          git add . # Stage all changes
          git commit -m "Auto-generated files" # Commit changes
          git push https://${{ secrets.PAT_TOKEN }}@github.com/${{ github.repository }}  # Push changes to the new branch using PAT
